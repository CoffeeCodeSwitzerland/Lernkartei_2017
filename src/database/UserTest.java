package database;

public class UserTest {

	public static void main(String[] args) {
		
		// Neue Instanz erstellen, ZB: Database j = new Database();
		
		// Benötigt deinen String in ()
		// j.checkPossible();
		
		// if true, insert (mit j.insert(String mit values, boolean ob lehrer oder nicht), else errormessage an user
		// j.delUser(String values, boolean ob Lehrer odr schüler
		
	}

}
